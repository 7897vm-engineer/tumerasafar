"use client"; import { useRouter } from "next/navigation"; export default function LogoutButton(){const router=useRouter();return <button onClick={async()=>{const apiBase=process.env.NEXT_PUBLIC_API_URL?.replace(/\/$/,"");if(apiBase)await fetch(`${apiBase}/api/auth/logout`,{method:"POST",credentials:"include"});router.push("/");router.refresh()}} className="rounded-full border border-white/25 px-4 py-2 text-sm font-bold">Sign out</button>}
