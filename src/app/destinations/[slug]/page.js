import { notFound } from "next/navigation"; import Link from "next/link"; import { destinations, packages } from "@/lib/catalog";
export default async function Destination({params}){const {slug}=await params;const d=destinations.find(x=>x.slug===slug);if(!d)return notFound();return <main className="min-h-screen bg-[#f8f7f2]"><div className="h-[440px] bg-cover bg-center" style={{backgroundImage:`linear-gradient(rgba(5,45,44,.52),rgba(5,45,44,.52)),url(${d.image})`}}><div className="mx-auto max-w-6xl px-6 py-8 text-white"><Link href="/destinations">← Destinations</Link><h1 className="mt-64 font-serif text-6xl">{d.name}</h1></div></div><div className="mx-auto max-w-4xl px-6 py-16"><p className="eyebrow">{d.type} journeys</p><h2 className="mt-3 font-serif text-4xl">{d.tagline}</h2><p className="mt-6 max-w-2xl leading-8 text-slate-600">We believe a destination is best understood slowly. Our journeys here pair essential landmarks with local knowledge, comfortable stays and room to follow your curiosity.</p><Link href={`/packages?search=${d.name}`} className="mt-8 inline-block rounded-full bg-[#d4664d] px-6 py-3 text-sm font-bold text-white">Explore {d.name} packages</Link></div></main>}
