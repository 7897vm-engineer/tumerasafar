"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import LogoutButton from "@/components/auth/LogoutButton";
export default function Account() { const router = useRouter(); const [user, setUser] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { const apiBase = process.env.NEXT_PUBLIC_API_URL?.replace(/\/$/, ""); if (!apiBase) { router.replace("/sign-in"); return; } fetch(`${apiBase}/api/auth/me`, { credentials: "include" }).then((response) => response.ok ? response.json() : Promise.reject()).then((data) => setUser(data.user)).catch(() => router.replace("/sign-in")).finally(() => setLoading(false)); }, [router]); if (loading) return <main className="min-h-screen bg-[#f8f7f2] p-10 text-center text-slate-500">Loading your account…</main>; if (!user) return null; return <main className="min-h-screen bg-[#f8f7f2]"><header className="bg-[#073b3a] px-6 py-6 text-white"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href="/" className="font-serif text-2xl">tumerasafar<span className="text-[#f4c96a]">.</span></Link><LogoutButton /></div></header><section className="mx-auto max-w-6xl px-6 py-16"><p className="eyebrow">Traveller account</p><h1 className="mt-3 font-serif text-5xl">Hello, {user.name}.</h1><p className="mt-4 text-slate-600">Your saved travel life starts here.</p><div className="mt-10 grid gap-5 md:grid-cols-3"><div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">Upcoming journeys</p><p className="mt-3 font-serif text-4xl">0</p><Link className="mt-5 inline-block text-sm font-bold text-teal-800" href="/packages">Explore packages →</Link></div><div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">Enquiries</p><p className="mt-3 font-serif text-4xl">0</p><Link className="mt-5 inline-block text-sm font-bold text-teal-800" href="/enquire">Plan a trip →</Link></div><div className="rounded-2xl bg-[#e7efe9] p-6"><p className="text-sm text-slate-500">Account email</p><p className="mt-3 font-bold">{user.email}</p></div></div></section></main>; }
