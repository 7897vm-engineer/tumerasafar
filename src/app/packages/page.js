import Link from "next/link"; import { packages } from "@/lib/catalog"; import { ArrowRight } from "lucide-react";
export const metadata={title:"Tour packages"};
export default function Packages({searchParams}){const search=(searchParams?.search||"").toLowerCase();const shown=packages.filter(p=>!search||`${p.name} ${p.category} ${p.route}`.toLowerCase().includes(search));return <main className="min-h-screen bg-[#f8f7f2]"><div className="bg-[#073b3a] px-6 py-16 text-white"><Link href="/" className="font-serif text-2xl">tumerasafar<span className="text-[#f4c96a]">.</span></Link><p className="mt-16 text-xs font-bold uppercase tracking-[.2em] text-[#f4c96a]">Our collection</p><h1 className="mt-3 font-serif text-5xl">Journeys designed to linger.</h1></div><section className="mx-auto max-w-6xl px-6 py-16"><p className="mb-8 text-sm text-slate-500">{shown.length} handpicked journeys {search&&`for “${search}”`}</p><div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{shown.map(p=><article key={p.slug} className="overflow-hidden rounded-3xl bg-white shadow-sm"><div className="h-56 bg-cover bg-center" style={{backgroundImage:`url(${p.image})`}}/><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-teal-700">{p.category} · {p.duration}</p><h2 className="mt-3 font-serif text-2xl">{p.name}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{p.route}</p><Link href={`/packages/${p.slug}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#d4664d]">See the journey <ArrowRight size={16}/></Link></div></article>)}</div></section></main>}
