import express from "express"; import cors from "cors"; import helmet from "helmet"; import rateLimit from "express-rate-limit"; import { connectDB } from "./config/db.js"; import enquiryRoutes from "./routes/enquiries.js";
const app=express();app.use(helmet());app.use(cors({origin:process.env.WEB_ORIGIN?.split(",")||"http://localhost:3000"}));app.use(express.json({limit:"20kb"}));app.use("/api",rateLimit({windowMs:15*60*1000,max:150}));app.get("/health",(_,res)=>res.json({status:"ok"}));app.use("/api/enquiries",enquiryRoutes);app.use((err,_,res,__)=>(console.error(err),res.status(500).json({error:"Internal server error"})));connectDB().then(()=>app.listen(process.env.API_PORT||4000,()=>console.log("API ready"))).catch(console.error);
