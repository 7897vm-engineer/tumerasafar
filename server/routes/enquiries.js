import { Router } from "express"; import { z } from "zod"; import Enquiry from "../models/Enquiry.js"; const router=Router();const schema=z.object({name:z.string().min(2),email:z.string().email(),phone:z.string().min(7),message:z.string().max(1200).optional()});router.post("/",async(req,res,next)=>{try{const data=schema.parse(req.body);res.status(201).json({success:true,enquiry:await Enquiry.create(data)})}catch(e){if(e instanceof z.ZodError)return res.status(400).json({error:"Invalid enquiry",fields:e.flatten()});next(e)}});export default router;
